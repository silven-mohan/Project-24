{
  "name": "nextapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "clean": "taskkill /F /IM node.exe /T 2>nul || exit 0",
    "predev": "npm run clean",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "backend": "workspace:*"
  }
}
